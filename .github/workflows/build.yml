- name: Decrypt and copy keystore
  env:
    KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
  run: |
    echo "$KEYSTORE_BASE64" | base64 -d > $GITHUB_WORKSPACE/tv/keystore.p12  # 更改为 .p12
    test -f $GITHUB_WORKSPACE/tv/keystore.p12 || exit 1

- name: Build with Gradle
  env:
    STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
    KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
    KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
  run: |
    ./gradlew :tv:assembleRelease \
      -Pandroid.injected.signing.store.file=$GITHUB_WORKSPACE/tv/keystore.p12  # 更改为 .p12
      -Pandroid.injected.signing.store.password=$STORE_PASSWORD \
      -Pandroid.injected.signing.key.alias=$KEY_ALIAS \
      -Pandroid.injected.signing.key.password=$KEY_PASSWORD
